// Write a function that accepts a string and a number, x and y. It will return a new string with x repeated y times.

// repeat("hi", 3); // returns "hihihi"


function repetedXtimes (x , y){
    let newString = ""
    for (let y = 0; y <100; y ++){
        console.log(y);
        if (typeof x === "String" && typeof y === "Number"){
        return newString * y;
      
        
}
else {
    return false
}
        
    }

     
}
repetedXtimes ("hello" , 3);
