function fcnVowels (string){
    let countVowels = ""; 
    for (i = 0; i < string.length ; i++){
    if (string[i]==="a"){
        if (string[i]==="e"){
            if (string[i]==="i"){
                if (string[i]==="o"){
                    if (string[i]==="u"){
            
                    }  
            }  
        }
    }


    // else if (string[i]==="b");

    // }    &&string[i]==="e"&&string[i]==="i"&&string[i]==="o"&&string[i]==="u"){
        }else if {    countVowels += string.length[i];
    }
  
    }

    return countVowels
}
   
console.log(fcnVowels('hhheeehehehehehaammmskskksks'))



// Write a function that accepts a string as an argument. It will return the number of vowels within the string.